# =============================================================================
# MM Rebate Bot - Environment Configuration
# =============================================================================
#
# SECURITY NOTICE:
# ----------------
# For PRODUCTION deployments, use Docker Secrets instead of environment variables!
# See docs/DOCKER_SECRETS_SETUP.md for secure configuration guide.
#
# This .env file is for DEVELOPMENT/TESTING only.
# NEVER commit real API keys to version control!
# =============================================================================

# Bybit API Configuration
# -----------------------
# PRODUCTION: Use Docker Secrets (see docs/DOCKER_SECRETS_SETUP.md)
# DEV/TEST: Uncomment and fill these values
#
# BYBIT_API_KEY=your_dev_api_key_here
# BYBIT_API_SECRET=your_dev_api_secret_here
#
# Or use file-based secrets (recommended for local dev):
# BYBIT_API_KEY_FILE=./secrets/bybit_api_key
# BYBIT_API_SECRET_FILE=./secrets/bybit_api_secret

# Environment Selection
USE_TESTNET=true

# Bybit REST API Endpoints
REST_TESTNET=https://api-testnet.bybit.com
REST_MAINNET=https://api.bybit.com

# Bybit WebSocket Endpoints
WS_PUBLIC_TESTNET=wss://stream-testnet.bybit.com/v5/public
WS_PRIVATE_TESTNET=wss://stream-testnet.bybit.com/v5/private
WS_PUBLIC_MAINNET=wss://stream.bybit.com/v5/public
WS_PRIVATE_MAINNET=wss://stream.bybit.com/v5/private

# Storage Configuration
STORAGE_BACKEND=parquet
STORAGE_SQLITE_PATH=./data/market_maker.db
STORAGE_PG_HOST=localhost
STORAGE_PG_PORT=5432
STORAGE_PG_DATABASE=market_maker
STORAGE_PG_USERNAME=market_maker
# PRODUCTION: Use Docker Secrets
# STORAGE_PG_PASSWORD_FILE=/run/secrets/postgres_password
# DEV/TEST: Use env var
STORAGE_PG_PASSWORD=your_password_here
STORAGE_PG_SCHEMA=public

# Database Configuration (Optional)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=market_maker
POSTGRES_USER=market_maker
# PRODUCTION: Use Docker Secrets (postgres_password)
POSTGRES_PASSWORD=your_password_here

# Monitoring
PROMETHEUS_PORT=8000
GRAFANA_PORT=3000

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json
